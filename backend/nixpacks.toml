# IAskan Backend - Nixpacks configuration for Railway

[phases.setup]
nixPkgs = ["python311", "postgresql_15"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m uvicorn server:app --host 0.0.0.0 --port ${PORT:-8001}"

[variables]
PYTHON_VERSION = "3.11"
